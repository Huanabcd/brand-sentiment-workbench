# 安全说明

请不要在公开 Issue 中提交客户内容、账号、Cookie、Token、许可证、安装标识、数据库、日志或包含本机路径的截图。

软件更新只接受固定项目 Releases 中的文件，并使用首装包内置的独立 Ed25519 发布公钥验证签名。GitHub 的 `latest` 标记仅用于发现版本，不作为信任依据。

当前 owner-QA 更新根：`update-owner-qa-2026-01`；Ed25519 SPKI SHA-256 指纹为 `e8577ff7b530fafa7ce5e4e7166e8c8aea24d0655e269388ef8bffe13f4a3dc0`。机器可读公钥见 `update-root.json`，私钥不在本仓库、发布资产或客户端中。

若发现可导致更新签名绕过、任意文件写入、许可证绕过或客户数据泄露的问题，请先通过与项目所有者约定的私下渠道报告，待修复后再公开细节。
